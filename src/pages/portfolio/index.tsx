import { PortfolioContainer } from "./elements";

export interface PortfolioProps extends React.HTMLProps<HTMLDivElement> {}

export function Portfolio({...props}: PortfolioProps) {
    return (
        <PortfolioContainer>

        </PortfolioContainer>
    );
}